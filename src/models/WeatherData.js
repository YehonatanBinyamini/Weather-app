class WeatherData {
    constructor(city, temperature, description, icon, key) {
        this.city = city;
        this.temperature = temperature;
        this.description = description;
        this.icon = icon;
        this.key = key;
    }
}

export default WeatherData;